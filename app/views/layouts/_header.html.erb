<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "tikkets", "/", :class => "navbar-brand" %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <p class="navbar-text"></p>
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li class="<%= 'active' if current_page?(root_path) or current_page?(user_tickets_path(current_user))%>">
            <%= link_to 'mes tickets', user_tickets_path(current_user) %>
          </li>
        <% end %>
        <% if current_user and current_user.admin? %>
          <li class="<%= 'active' if current_page?(tickets_path) %>"><%= link_to "tous les tickets", tickets_path %></li>
          <li class="<%= 'active' if current_page?(activities_path) %>"><%= link_to 'timeline', activities_path %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if not user_signed_in? %>
          <li><%= link_to('connexion', new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              <%= current_user.username %>  <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to('déconnexion', destroy_user_session_path, :method => :delete) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>